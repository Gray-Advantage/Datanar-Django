[tool.flake8]
max-line-length = 79
inline-quotes = "double"
import-order-style = "google"
per-file-ignores = [
    "*/migrations/*:E501,LN001,C812,DALL000",
]
application-import-names = [
    "api",
    "dashboard",
    "core",
    "datanar",
    "homepage",
    "qr_codes",
    "redirects",
    "statistic",
    "users",
]

[tool.black]
line-length = 79

[tool.ruff]
line-length = 79
target-version = "py39"
extend-exclude = ["migrations"]

[tool.ruff.format]
indent-style = "space"
quote-style = "double"

[tool.ruff.lint]
select = [
    "A",
    "COM",
    "DJ",
    "E",
    "ERA",
    "F",
    "FIX",
    "I",
    "ISC",
    "N",
    "PTH",
    "Q",
    "RET",
    "RSE",
    "T20",
    "UP",
    "W",
]

[tool.ruff.lint.isort]
force-sort-within-sections = true
order-by-type = false
known-first-party = [
    "api",
    "dashboard",
    "core",
    "datanar",
    "homepage",
    "qr_codes",
    "redirects",
    "statistic",
    "users",
]

[tool.ruff.lint.flake8-quotes]
docstring-quotes = "double"
inline-quotes = "double"
multiline-quotes = "double"
